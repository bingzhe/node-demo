extends layout

block content
    h1= title
    p Fill in the form below to sign in！
    form(
        action='/login'
        method='post'
    )
        p
            input(
                type='text'
                name='username'
                placeholder='Username'
            )
        p
            input(
                type='password'
                name='password'
                placeholder='Password'
            )
        p
            input(
                type='submit'
                value='Login'
            )
        